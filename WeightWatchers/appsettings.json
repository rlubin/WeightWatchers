{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "ConnectionStrings": {
    //when starting backend with docker-compose to connec to sql server in container
    "WeightWatchers": "Server=sqlserver,1433;Database=WeightWatchers;User Id=sa;Password=yourStrong(!)Password;TrustServerCertificate=True"

    //when starting backend with http or https to connect to sql server on local host
    //"WeightWatchers": "Data Source=DESKTOP-HSHM0PP;Initial Catalog=WeightWatchers;Integrated Security=True;Encrypt=True;Trust Server Certificate=True"

    //when starting backend in VS via Dockerfile to connect to sql server on local host
    //"WeightWatchers": "Server=host.docker.internal,1433;Database=WeightWatchers;User Id=weightwatchers;Password=WeightWatchersPassword123;TrustServerCertificate=True"
  },
  "AllowedHosts": "*"
}
